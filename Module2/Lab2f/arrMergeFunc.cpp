#include <iostream>

int main() {

    return 0;
}

template <class T>
T* arrMerger(T* arr1, int size1, T* arr2, int size2) {
    T* mergedArr[size1 + size2] = arr1;

    for (int i = 0; i < size2; ++i) {
        mergedArr[size1] = 
    }
}